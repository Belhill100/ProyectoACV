#include "View.h"
#include "Archivo.h"

View::View(){
}

void View::mostrarMenu(){
    int opcion = -1;
    do
    {
        // TODO completar
        switch (opcion)
        {
            case 0:
                break;
        }

    } while (opcion != 0);
}
